package D7;

public class Ques {
    public static void main(String[] args) {

   /*     // 7 area of parameter of rectangle

        int l= 40;
        int b= 20;
        int parameter= 2*(l*b);
        System.out.println(parameter);
*/
/*        // 8 compound interest

        double p = 100000;
        int t = 15;
        double rate = 18;
        double amt = p* Math.pow(((100+rate)/100),t);
        System.out.println(amt);
*/
/*        // 9 three side triangle by herons formula

        int a=2,b=3,c=4;
        int s = (a+b+c)/2;

        int ans = Math.cbrt(s(s-a)*s(s-b)*s(s-c));
        System.out.println(ans);
*/

/*        // 10 sarface area of sphare

        double r=2;
        double ans = (Math.pow(r,2));
        double d = 4*Math.PI;
        System.out.println(ans*d);
        //or//  System.out.println((4*Math.PI)*(Math.pow(r,2)));
*/

        // 11 circumference and area of circle

        double r = 4;
        System.out.println("circumference is =" + (2 * Math.PI) * r);
        System.out.println("area is =" + (Math.PI) * (Math.pow(r, 2)));

    }
}
